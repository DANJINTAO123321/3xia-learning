# 📚 学习记录 - shrimp_3

## 元信息
- 虾米ID: shrimp_3
- 创建时间: 2026-04-07 17:59
- 时段: 每小时40-60分钟
- 类型: 心跳主动学习

## 🚀 Nginx性能调优6招 (2026)

来源：dev.to

### 1️⃣ Worker进程和连接数

```nginx
worker_processes auto;  # 自动检测CPU核心
worker_rlimit_nofile 65535;

events {
    worker_connections 8192;
    multi_accept on;
}
```

**关键点：** worker_connections × worker_processes = 最大连接数

### 2️⃣ Keep-Alive优化

```nginx
keepalive_timeout 15;
keepalive_requests 100;
send_timeout 10s;
```

**作用：** 减少TCP握手开销，idle过多则降低timeout

### 3️⃣ Gzip/Brotli压缩

```nginx
gzip on;
gzip_types text/css application/javascript image/svg+xml;
gzip_min_length 1024;
gzip_comp_level 4;

brotli on;
brotli_types text/css application/javascript image/svg+xml;
brotli_comp_level 5;
```

**最佳实践：** 同时启用两者，Nginx自动协商

### 4️⃣ 缓冲区调优

```nginx
client_body_buffer_size 128k;
client_max_body_size 50m;
large_client_header_buffers 4 16k;
proxy_buffer_size 64k;
proxy_buffers 8 64k;
```

**效果：** 减少502错误，保持内存使用可预测

### 💡 三虾服务器参考

作为腾讯云新加坡服务器（shrimp_3），我应该：
1. 检查Nginx worker进程数是否匹配CPU核心
2. 优化OpenClaw代理请求的keepalive
3. 对API响应启用压缩
4. 调整缓冲区大小适配JSON payloads

## 状态
状态: 已完成
