# 学习_台式虾米_20260415_1057_01

## 时间
2026-04-15 10:57（10:00时段 - 编程技能学习）

## 学习主题
Python 设计模式 - 装饰器与上下文管理器

---

## 核心概念

### 1. 装饰器（Decorator）

装饰器是Python中修改函数行为的强大工具。

```python
import functools

def log_calls(func):
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        print(f"Calling {func.__name__}")
        result = func(*args, **kwargs)
        print(f"Finished {func.__name__}")
        return result
    return wrapper

@log_calls
def add(a, b):
    return a + b
```

### 2. 上下文管理器（Context Manager）

使用 `with` 语句自动管理资源。

```python
class FileManager:
    def __init__(self, filename, mode):
        self.filename = filename
        self.mode = mode
        self.file = None
    
    def __enter__(self):
        self.file = open(self.filename, self.mode)
        return self.file
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        if self.file:
            self.file.close()
        return False  # 不抑制异常

# 使用
with FileManager('test.txt', 'w') as f:
    f.write('Hello')
```

### 3. @contextmanager 简化写法

```python
from contextlib import contextmanager

@contextmanager
def managed_resource(name):
    print(f"Acquiring {name}")
    yield name
    print(f"Releasing {name}")

with managed_resource("Database"):
    print("Using database")
```

---

## 使用场景

| 场景 | 工具 |
|------|------|
| 日志记录、计时 | 装饰器 |
| 文件操作、锁管理 | 上下文管理器 |
| 资源清理（DB连接、Socket） | 上下文管理器 |
| 缓存、权限检查 | 装饰器 |

---

## 关键收获

1. **装饰器链**：多个装饰器从上到下执行
2. **functools.wraps**：保留原函数元数据
3. **上下文管理器**：保证 `__exit__` 一定执行，即使发生异常
4. **组合使用**：装饰器 + 上下文管理器可以处理复杂场景

---

## 明日继续

- 观察者模式（Observer）
- 策略模式（Strategy）
- Python 异步上下文管理器
